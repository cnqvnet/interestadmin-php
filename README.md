# interestadmin - 企业级PHP后台管理系统

## 📋 项目描述
interestadmin-php 是一个基于自主开发MVC框架的企业级PHP后台管理系统，采用现代化技术栈，具备高度可扩展性、安全性和可维护性。系统支持多数据库、模块化开发和插件生态，适用于各种规模的企业应用。

### 核心特性
- 🔒 **企业级安全**：JWT+OAuth双认证，CSRF/XSS/SQL注入全面防护
- 🗄️ **多数据库兼容**：MySQL 8.0 / SQLite / PostgreSQL 18+ 无缝切换
- 🔌 **插件化架构**：Hook机制+ServiceProvider，支持热插拔扩展
- 📱 **多终端适配**：Bootstrap 5.3.8响应式布局，支持PC/平板/移动端
- ⚡ **高并发性能**：路由缓存+视图编译+查询优化，QPS支持3000+

### 适用场景
- 企业内部管理系统
- SaaS平台后台
- 内容管理系统(CMS)
- 数据监控与分析平台
- 电商/支付后台管理系统

## 🛠️ 技术栈

### 后端技术
- **PHP 8.4**：充分利用JIT编译器、联合类型、命名参数等新特性（本地路径：D:\phpEnv\php\php-8.4，必须使用此版本）
- **数据库支持**：
  - MySQL 8.0（支持DDL原子化、秒加列等特性）
  - SQLite 3.35+
  - PostgreSQL 18+
- **自主MVC框架**：路由、控制器、模型、视图分离
- **自主模板引擎**：支持模板继承、变量输出、条件判断

### 前端技术
- **Bootstrap 5.3.8**（本地化部署）
- **Font Awesome 7.1.0**（本地化部署）
- **Axios**：异步请求处理
- **Chart.js**：数据可视化
- **Webpack 5**：资源打包与构建
- **ESLint + Prettier**：代码质量控制

### 开发工具
- **Composer**：PHP依赖管理
- **PHPUnit**：单元测试
- **Git**：版本控制

### 安全认证
- **JWT认证**：无状态令牌认证
- **OAuth 2.0**：第三方登录支持
- **双重认证机制**：确保系统安全

## 📁 目录结构

```

├── app/                        # 应用核心目录
│   ├── Controllers/            # 控制器层
│   ├── Models/                 # 模型层
│   ├── Views/                  # 视图层
│   ├── Middleware/             # 中间件
│   ├── Services/               # 服务层
│   ├── Translations/           # 多语言翻译文件
│   │   ├── en_US.php           # 英语(美国)
│   │   ├── zh_CN.php           # 简体中文
│   │   ├── zh_TW.php           # 繁体中文
│   │   └── loader.php          # 翻译加载器
│   └── Validators/             # 表单验证规则
├── bootstrap/                  # 启动引导
│   └── cache/                  # 启动缓存目录
├── config/                     # 配置文件
│   ├── app.php                 # 应用配置
│   ├── database.php            # 数据库配置
│   ├── auth.php                # 认证配置
│   └── language.php            # 语言配置
├── core/                       # 框架核心
│   ├── Router.php              # 路由解析
│   ├── Database.php            # 数据库抽象层
│   ├── Auth.php                # 认证服务
│   ├── TemplateEngine.php      # 模板引擎
│   ├── Language.php            # 语言切换与翻译服务
│   └── Validator.php           # 验证器核心
├── docs/                       # 文档目录
├── helpers/                    # 辅助函数
│   ├── array.php               # 数组处理函数
│   ├── string.php              # 字符串处理函数
│   ├── file.php                # 文件处理函数
│   └── security.php            # 安全相关函数
├── migrations/                 # 数据库迁移文件
├── plugins/                    # 插件目录
├── public/                     # Web根目录
│   ├── index.php               # 入口文件
│   ├── assets/                 # 静态资源
│   │   ├── css/
│   │   ├── js/
│   │   ├── fonts/
│   │   └── i18n/               # 前端多语言文件
│   │       ├── en.json
│   │       └── zh.json
│   └── install/                # 安装程序
├── seeds/                      # 数据填充文件
├── storage/                    # 存储目录
│   ├── logs/                   # 日志文件
│   ├── cache/                  # 缓存文件
│   ├── backups/                # 数据库备份
│   └── uploads/                # 上传文件存储
└── tests/                      # 测试文件
 
```

## 🔧 功能模块详解

### 模块1：仪表盘模块 (Dashboard)
- **数据统计**：用户增长曲线、登录统计、资源使用量（支持30/90/365天范围）
- **可视化图表**：ECharts集成，支持饼图、折线图、柱状图、雷达图
- **待处理通知**：系统公告、用户反馈、安全告警（支持已读/未读标记）
- **模块展示区**：动态加载已安装插件的快捷入口，支持拖拽排序

### 模块2：用户管理模块 (User Management)
- **RBAC权限模型**：支持无限极权限节点，权限细粒度到按钮级别
- **角色管理**：角色CRUD、权限分配、数据范围控制（全部/本级/自定义）
- **用户列表**：分页查询、高级搜索（用户名/邮箱/角色/状态）、批量操作
- **用户管理**：
  - 添加：用户名、邮箱、手机号、角色、部门、密码复杂度验证
  - 编辑：基本信息、状态（正常/禁用）、密码重置
  - 删除：软删除+硬删除，关联数据清理
  - 导入/导出：Excel模板导入，用户数据导出
  
### 模块3：生态应用模块 (Ecosystem)
- **插件架构**：
  - Hook钩子：系统启动/路由加载/视图渲染等30+注入点
  - ServiceProvider：插件服务注册机制
  - 插件配置：每个插件独立config.json，支持配置界面自动生成
- **插件市场**：插件列表、在线购买、本地安装、版本更新
- **应用管理**：支持APP/小程序/H5的多端配置，主题色、Logo、导航栏自定义

### 模块4：安全管理模块 (Security)
- **SQL入侵管理**：基于正则+语法树的注入检测，异常SQL拦截日志
- **数据库管理**：phpMyAdmin-like界面，支持表结构查看、SQL执行、数据导出
- **操作日志**：记录操作人、时间、IP、UA、请求参数，支持日志归档与清理
- **登录日志**：失败锁定机制（5次失败锁定15分钟）

### 模块5：资源管理模块 (Assets)
- **图片管理**：缩略图自动生成（支持webp/avif格式），水印添加，批量删除
- **文件管理**：目录树浏览，文件上传（分片上传+断点续传），权限控制，在线预览（PDF/图片/文本）
- **存储驱动**：本地存储/阿里云OSS/腾讯云COS/七牛云（插件化扩展）

### 模块6：系统设置模块 (Settings)
- **网站设置**：站点名称、域名备案、SEO标题/关键词/描述、统计代码
- **上传设置**：允许类型（白名单）、大小限制、存储路径规则（按日期/随机）
- **邮件设置**：SMTP配置、发送测试、邮件模板管理（支持队列）
- **短信设置**：阿里云短信/腾讯云短信接口，验证码模板，发送记录
- **OAuth设置**：微信/QQ/Github/Google登录配置，回调管理


## 🚀 安装步骤

### 环境要求
- PHP >= 8.4.2 with PDO, OpenSSL, Mbstring, Tokenizer
- Web Server: Apache/Nginx with URL Rewrite
- Database: MySQL 8.0+ / SQLite 3.40+ / PostgreSQL 18+
- Redis 7.0+ (可选，用于缓存与会话)

### 一键安装流程
1. **上传文件**：将代码上传至Web目录，设置`public`为根目录
2. **目录权限**：确保`storage/`、`bootstrap/cache/`、`public/uploads/`可写（755）
3. **访问安装**：浏览器访问 `http://your-domain.com/install.php`
4. **按照向导**：5步完成安装（许可->环境检测->系统设置->数据库配置->完成）
5. **安全加固**：安装完成后删除安装文件

### 手动安装步骤
1. **克隆代码**：`git clone https://github.com/cnqvnet/interestadmin-php.git`
2. **安装依赖**：`cd enterprise-php-admin && composer install`
3. **配置文件**：复制`.env.example`为`.env`并配置相关参数
4. **生成密钥**：`php artisan key:generate`（框架命令）
5. **数据库迁移**：`php artisan migrate`（框架命令）
6. **创建管理员**：`php artisan admin:create`（框架命令）

### 使用 Docker 部署

#### Docker Compose 部署
1. **克隆代码**：
   ```bash
   git clone https://github.com/cnqvnet/interestadmin-php.git
   cd interestadmin-php
   ```

2. **配置环境变量**：
   ```bash
   cp .env.example .env
   # 根据需要编辑 .env 文件
   ```

3. **启动服务**：
   ```bash
   docker-compose up -d
   ```

4. **访问系统**：
   浏览器访问 `http://localhost:8080` 开始安装向导

#### Docker 环境说明
- **PHP 容器**：预装所有必要扩展，使用 PHP-FPM 8.4
- **Nginx 容器**：预配置的 Nginx 服务，包含重写规则
- **MySQL 容器**：MySQL 8.0，数据持久化存储
- **Redis 容器**：用于缓存和会话管理（可选）

### 传统服务器部署

#### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/interestadmin-php/public;

    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass php-fpm:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
```

#### Apache 配置示例
确保启用了 `mod_rewrite` 模块，并在网站根目录放置以下 `.htaccess` 文件：
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
```

### 常见问题
- **权限错误**：确保所有需要写入的目录权限正确设置为755
- **数据库连接失败**：检查`.env`文件中的数据库配置
- **URL重写问题**：确保Apache启用了`mod_rewrite`或Nginx配置了正确的重写规则
- **Docker连接问题**：确保容器间网络配置正确，可通过`docker-compose logs`查看详细日志

## 🤝 贡献指南

我们欢迎社区贡献！如果你想为项目做出贡献，请按照以下步骤操作：

1. Fork本项目
2. 创建功能分支：`git checkout -b feature/amazing-feature`
3. 提交更改：`git commit -m 'Add some amazing feature'`
4. 推送到分支：`git push origin feature/amazing-feature`
5. 打开一个Pull Request

### 代码规范
- 遵循PSR-12编码标准
- 为新增功能编写单元测试
- 保持代码注释的完整性
- 提交前运行代码检查工具

## 📜 许可证

本项目采用MIT许可证 - 详情请查看LICENSE文件

## 📧 联系方式

- 项目主页：https://github.com/cnqvnet/interestadmin-php
- 问题反馈：https://github.com/cnqvnet/interestadmin-php/issues

## 📝 更新日志

### v0.1.0 (已完成)
- 基础框架完成
- 用户管理模块完成
- 内容管理模块（文章、分类、标签）完成
- 安全与认证系统（密码安全、CSRF防护、XSS过滤等）完成
- 安全管理模块（安全加固、漏洞检测、SQL注入防护）完成
- 测试与优化阶段完成（单元测试、功能测试、安全测试、性能优化）
- 性能基准测试工具完成
- 测试辅助工具完成

### v1.0.0 (预计2026-02-04)
- 初始正式版本发布
- 包含全部核心功能模块
- 支持多数据库、多语言、插件化架构
